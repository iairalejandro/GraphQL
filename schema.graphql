
type Query {
    hello: String!
    greeting(name: String!): String!
    getPerson(id: ID!): Person
    monthName(monthNumber: Int!): String!
}

type Person {
  id: ID!
  name: String!
  age: Int!
}
